#include <string.h>

static char* prev = NULL;

char* strtok (char* __restrict str, const char* __restrict delim){
    if(str==NULL){

    } else {

    }

    // todo: implement strtok, it's very possible without malloc
    return NULL;
}
